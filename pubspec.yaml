name: petlovers
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  petlovers_ui:
    path: packages/petlovers_ui

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: false
